// src/server/services/userService.ts

// import User, { IUser } from '../models/Users';

// export async function createUser(
//   name: string,
//   userId: string,
//   department: string,
//   _id: string,
//   image: string
// ): Promise<IUser> {
//   const user = new User({ name, userId, department, _id, image });
//   await user.save();
//   return user;
// }

// Additional service functions can be added here as needed
